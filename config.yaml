engine:
  engine_type: "in_memory"
network:
  address: "localhost:8829"
  max_connections: 100
  max_message_size: 8000
  idle_timeout: 10m
logging:
  server_level: "info"
  cli_output: "cli.log"
  server_output: "server.log"
